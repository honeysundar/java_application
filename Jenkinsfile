pipeline {
    agent {  label 'worker-linux' }
    stages {
        stage('Build') { 
             steps {
                sh 'mvn clean package'
        }
        }
       
        
     }
        
    }
