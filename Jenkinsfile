pipeline {
    agent {  label 'worker_linux' }
    stages {
        stage('Build') { 
             steps {
                sh 'mvn clean package'
        }
        }
       
        
     }
        
    }
